#volModel
  #container
    #topLoader
  .switch.am-fr
    input.cmn-toggle.cmn-toggle-round#cb-12(type="checkbox" value="volume" data-bind="checked:$root.switchValue(), event:{ change: $root.volChanged}")
    label(for="cb-12")
script.
  var vlVM = new volumeViewModel();
  setTimeout(vlVM.loaddata, 200);
  ko.applyBindings(vlVM, document.getElementById("volModel"));
  mainmodel.showback(true);