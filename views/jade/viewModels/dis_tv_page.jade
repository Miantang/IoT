#tvModel
  //.switch.am-fr
  //  input.cmn-toggle.cmn-toggle-round#tv-9(type="checkbox" value="tv" data-bind="checked:$root.switchValue(), event:{ change: $root.togSwitched}")
  //  label(for="tv-9")
  //.am-list-news.am-list-news-default(data-am-widget="list_news")
  ul.inner-boxes.tvboxes.am-thumbnails(data-bind="foreach: { data: tvButtonGroup, as: 'dv' }")
    li.inner-box.tvbox.am-thumbnail.am-btn(data-bind="text:dv.name,attr: {id: 'tvbutton-' + dv.id, value : dv.name }, click: $root.controlChanged, style:{height:$('#tvbutton-'+dv.id).width()*0.618+'px'}" data-am-button)
  //li.tvbox.am-thumbnail.am-btn(data-bind="text:$root.tvBtnGroup2[0].name,attr: {id: 'tvbutton-' + $root.tvBtnGroup2[0].id, value : $root.tvBtnGroup2[0].name }, click: $root.controlChanged" data-am-button)

script.
  var tvVM = new tvViewModel();
  setTimeout(tvVM.loaddata, 200);
  ko.applyBindings(tvVM, document.getElementById("tvModel"));
  mainmodel.showback(true);