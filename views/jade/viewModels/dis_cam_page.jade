#camModel
  center
    img(src="//192.168.1.116:8081/?action=stream")
  .switch.am-fr
    input.cmn-toggle.cmn-toggle-round#cb-6(type="checkbox" value="camera" data-bind="checked:$root.switchValue(), event:{ change: $root.camChanged}")
    label(for="cb-6")
script.
  var camVM = new camViewModel();
  setTimeout(camVM.loaddata, 200);
  ko.applyBindings(camVM, document.getElementById("camModel"));
  //go back to main page
  mainmodel.showback(true);